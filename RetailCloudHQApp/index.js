/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';

// Register the app component with the exact name from AndroidManifest
// This must match the app name in AndroidManifest.xml and MainActivity
AppRegistry.registerComponent('RetailCloudHQApp', () => App);
